{{-- <td>
                                            <form action="{{ route('admin.recibosnomina.destroy', $recibosnomina) }}" method="POST">
                                                @csrf
                                                @method('DELETE')
                                            <a class="btn btn-info btn-round btn-fab btn-fab-mini" href="{{ route('admin.recibosnomina.edit', $recibosnomina) }}"><i class="material-icons">edit</i></a>
                                            <button class="btn btn-danger btn-round btn-fab btn-fab-mini" onclick="return confirm('Estas seguro de Eliminar esta publicacion?')"><i class="material-icons">delete_forever</i></button>
                                            </form>
                                        </td> --}}